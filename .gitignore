# --- OS / editor ---------------------------------------------------------
.DS_Store
*.swp
.idea/
.vscode/

# --- Python -------------------------------------------------------------
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# --- Secrets / local config ------------------------------------------
.env
*.env
dataset/bts/          # download cookie jar
*cookies*

# --- Raw & intermediate data (regenerate: ingestion/ then processing/) --
dataset/bts_parquet/
storage/raw-zone/
storage/curated-zone/
storage/feature-zone/

# --- Analysis outputs -------------------------------------------------
# Regenerated by notebooks/PRT661_Flight_Delay_Full_Analysis.ipynb.
# The dashboard bundles its own copies of the figures / metrics / tuned
# hyper-parameters under dashboard/assets/, so this whole tree stays out of git.
PRT661_outputs/

# --- Dashboard: keep the committed sample + generated assets, drop the rest --
dashboard/data/*
!dashboard/data/.gitkeep
!dashboard/data/predictions.parquet
dashboard/data/predictions_full.parquet
*.part

# --- Misc ------------------------------------------------------------
*.log
