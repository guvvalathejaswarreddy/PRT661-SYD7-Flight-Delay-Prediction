# Copy to .env (git-ignored) and fill in real values. Never commit .env.
# Generate an "API client" under your OpenSky Network account settings to
# get these two values (OAuth2 client-credentials).
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
